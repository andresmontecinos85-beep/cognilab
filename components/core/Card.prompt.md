Generic soft surface container for grouping content.

```jsx
<Card>Contenido</Card>
<Card variant="tint" interactive>Tarjeta destacada con hover</Card>
```

Variants: default (white paper), `flat` (no shadow), `tint` (teal-tinted). Pass `interactive` for hover lift.
